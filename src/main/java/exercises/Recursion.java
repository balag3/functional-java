package exercises;

import com.googlecode.totallylazy.Sequence;

public class Recursion {

    /** computes factorial of a number recursively
     *
     * e.g.
     * 5! = 5 * 4 * 3 * 2 * 1
     * 3! = 3 * 2 * 1
     */
    public static Integer factorial(Integer number) {
        // TODO implement
        return 0;
    }

    /** computes fibonacci value for a given number recursively
     *
     * fibonacci sequence for number x can be obtained
     * by calculating fib(x - 1) + fib(x - 2)
     */
    public static Integer fibonacci(Integer number) {
        // TODO implement
        return 0;
    }

    /** returns true if 'sequence' contains 'needle'
     *
     * Tip: for learning purposes, implement sequence traversal with recursion
     * (have a look at head and tail)
     */
    public static <A> boolean contains(Sequence<A> seq, A needle) {
        // TODO implement
        return false;
    }


    /** returns the length of the given string without invoking the length
     * function of String or the size function of Sequence
     *
     * Tip: Convert string to Sequence with Sequence.characters(String s)
     */
    public static Integer length(String s) {
        // TODO implement
        return 0;
    }
}
